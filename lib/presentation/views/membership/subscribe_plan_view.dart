import 'package:eight_date_app/app/index.dart';

class SubscribePlanView extends StatelessWidget {
  const SubscribePlanView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Column(
        children: [kText36("abc")],
      ),
    );
  }
}
